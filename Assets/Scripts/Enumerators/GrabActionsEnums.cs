public enum GrabActionsEnums
{
    // When player misses a grab ray cast
    NOTHING,
    // When player hits a grab ray cast and the enemy is NOT attacking
    GRAB,
    // When player hits a grab ray cast and the enemy IS attacking
    COUNTER
}
